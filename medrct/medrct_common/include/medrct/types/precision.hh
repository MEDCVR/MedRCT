#pragma once
#include <math.h>

namespace medrct
{
// #Define here for compiler options in CMAKE.
using real_t = double;
// using real_t = float;

constexpr real_t PI = M_PI;
constexpr real_t PI_2 = M_PI / 2;

} // namespace medrct
